name: Starter Workflow

on:
  push:
    branches: 
      - main
  issues:
    types: [opened, reopened]
    
  schedule:
    - cron: '11 11 * * 5'
      
jobs:
  job1:
    runs-on: ubuntu-latest
    
    steps:
      - run: echo "Hello NDC Porto"
